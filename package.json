{
  "name": "location-client",
  "version": "1.0.0",
  "description": "location-client",
  "main": "index.js",
  "scripts": {
    "start-debug": "node --inspect --debug-brk location-consume.js",
    "start": "node location-consumer.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "avsc":"^5.2.3",
    "kafka-node":"^2.5.0"
  }
}
